apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: app-default
  namespace: default
spec:
  interval: 10m
  path: "./kubernetes/app"
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    branch: test
  targetNamespace: default