apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: production
resources:
  - mongodb-auth-setup.yaml

# patchesStrategicMerge:
#   - mongodb-resource-patch.yaml  # Disabled - can't patch operator-managed StatefulSet