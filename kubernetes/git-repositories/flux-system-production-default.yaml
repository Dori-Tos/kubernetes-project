apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: flux-system-production
  namespace: default
spec:
  interval: 1m0s
  ref:
    branch: production
  secretRef:
    name: flux-system
  url: https://github.com/Dori-Tos/kubernetes-project.git