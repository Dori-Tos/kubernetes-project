apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: default
spec:
  selector:
    app: movie-rating
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP  # Changed from NodePort to ClusterIP for internal access only