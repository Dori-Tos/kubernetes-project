apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: prod-movie-rating-policy
  namespace: production
spec:
  imageRepositoryRef:
    name: prod-movie-rating-repository
  policy:
    semver:
      range: ">=1.0.0"
